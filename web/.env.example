# Base URL of the Rails API.
# In development, Vite proxies /api requests to this target so the SPA
# can call relative paths (no CORS issues in dev).
# In production this is read by the bundle at build time and used directly
# by the axios client.
VITE_API_URL=http://localhost:3000
